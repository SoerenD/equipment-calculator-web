# Equipment Calculator Configuration

# Server Configuration
server.port=8080

# Equipment JSON URL Configuration
equipment.json.url=http://localhost
equipment.json.fallback.enabled=true

# Logging Configuration
logging.level.dl.equipmentCalculator=INFO
logging.level.org.springframework.web.client=DEBUG
